import React from 'react';

/**
 * Updated Speaking 모듈 레이아웃
 * - Drill / Review / Practice 모듈 통합
 */

export default function UpdatedSpeakingLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
