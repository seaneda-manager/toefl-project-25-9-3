import React from 'react';

/**
 * Speaking 2026 모듈 레이아웃
 * - 1024×768 고정 해상도 (실제 시험 환경)
 * - Forward-Only (뒤로가기 비활성)
 */

export default function Speaking2026Layout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
