// tailwind.config.ts (대략 이런 구조일 거야)
import type { Config } from "tailwindcss";

const config: Config = {
  content: [
    "./apps/web/app/**/*.{ts,tsx}",
    "./apps/web/components/**/*.{ts,tsx}",
    "./packages/ui/**/*.{ts,tsx}",
  ],
  theme: {
    extend: {
      colors: {
        brand: {
          // 👉 여기 색은 원하면 바꿔도 됨
          green: "#22c55e", // 대충 emerald-500 느낌. 원하면 학원 전용 색상 코드로 교체
        },
      },
    },
  },
  plugins: [],
};

export default config;
